# Crypto_TLS

This lab explores the fundamentals of Transport Layer Security (TLS) through hands-on implementation of a TLS client, server, and a simple HTTPS proxy. It demonstrates key concepts such as secure communication, certificate verification, and Man-in-the-Middle (MITM) attacks using Public-Key Infrastructure (PKI).
